{
  "name": "cc-controller",
  "private": true,
  "workspaces": [
    "web"
  ],
  "scripts": {
    "desktop": "npm run dev --prefix desktop",
    "web": "npm run dev --workspace=web"
  },
  "dependencies": {
    "@swc/helpers": "^0.5.5"
  }
}
